const chai = require('chai')
const expect = chai.expect

describe('Index2', () => {
  describe('#', () => {
    it('should output something', () => {
      const output = false
      expect(output).to.be.true
    })
  })
})